# OV5640 摄像头在 800×480 LCD 上显示异常分析报告

> **日期**: 2026-06-10（修订版 v2：MIPI 带宽已验证充足，聚焦 VIN 处理路径）
> **平台**: Renesas RA8P1 (Cortex-M85) + OV5640 + RTKLCDPAR1S00001BE (800×480 LCD)
> **软件框架**: FSP v6.4.x, LLVM 18.1.3

---

## 1. 故障现象与关键对照实验

| 观察维度 | 1024×600 目标 | QVGA (320×240) 目标 |
|----------|:-----------:|:-------------------:|
| **SDRAM 帧缓冲区内存** | ❌ 水平方向左右半一模一样（像素重复） | ✅ 完全正常 |
| **LCD 屏幕显示** | ❌ 横向重复；纵向底部 120 行缺失 | ✅ 正常 |
| **物理遮挡测试** | ❌ 左半边→有响应；右半边→**无反应** | ✅ 遮挡任意位置均有响应 |
| **VIN 缩放** | 关闭 (1:1 pass-through) | 使能 (UDS: 1024→320, 600→240) |

### ⚡ 核心推论

```
┌──────────────────────────────────────────────────────────────────────┐
│                                                                      │
│  QVGA 模式 = 同一 OV5640 配置 + 同一 MIPI 配置 + 同一 VIN 输入 +      │
│              VIN UDS 缩放使能 → 内存数据正常 ✅                        │
│                                                                      │
│  1024×600 = 同一 OV5640 配置 + 同一 MIPI 配置 + 同一 VIN 输入 +       │
│             VIN UDS 缩放关闭(1:1) → 内存数据重复 ❌                    │
│                                                                      │
│  → 摄像头源数据是正确的！MIPI 带宽是充足的！                           │
│  → 问题出在 VIN 的 1:1 bypass 路径上。                                │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 2. MIPI 带宽验证（已排除）

OV5640 PLL 时钟树的正确理解：

```
OV5640 PLL 时钟树（正确版本）：

  MIPI_XCLK (24MHz)
      │
      ▼
  PLL Pre-div (/8) → PLL Multiplier (×123) → PLL Root Div (×1, bypass)
      │
      ▼
  Base PLL = 24MHz / 8 × 123 = 369 MHz
      │
      ├── /mipi_clock_div (=2) → MIPI Clock  = 369/2 = 184.5 MHz
      │                              │
      │                              ▼
      │                         MIPI Data Rate = 184.5 × 2(DDR) × 2(lanes)
      │                                        = 738 Mbps
      │
      └── /pclk_root_div (=2)  → PCLK = 369/2 = 184.5 MHz (clamped to 96 MHz)
```

> **注意**：代码中 `camera_configure_clocks()` 的 `mipi_clk_hz = base_pll_hz / (mipi_clock_div * pclk_root_div)` 是**错误的**（将并行分频器误算为级联），但这个值**仅用于一致性检查**（`g_sys_clk_hz >= mipi_clk_hz`），不影响硬件寄存器配置。实际硬件使用并行分频。

| 分辨率 | 需要带宽 | 实际带宽 | 余量 | 结论 |
|--------|---------|---------|------|------|
| 1024×600 @60fps YUV422 | 1024×600×60×16 = **590 Mbps** | 184.5×2×2 = **738 Mbps** | **+20%** | ✅ 充足 |
| QVGA 320×240 @60fps | 320×240×60×16 = **74 Mbps** | 738 Mbps | +900% | ✅ 充足 |

**MIPI 带宽完全足够。瑞萨官方例程在同一配置下正常工作，进一步证实这一点。**

---

## 3. 根因分析：聚焦 VIN 1:1 Pass-through 路径

### 3.1 关键线索：QVGA vs 1024×600 的 VIN 路径差异

两者的唯一区别在于 `vin_scale_image()` 设置：

```c
// 1024×600 目标 (mipi_csi.c:82)
err = vin_scale_image(1024, 600);
// → input(1024×600) == output(1024×600) → scaling_enable = false
// → VIN UDS 被绕过，数据走 1:1 bypass 路径 → 内存出现重复

// QVGA 目标 (用户修改为)
err = vin_scale_image(320, 240);
// → input(1024×600) != output(320×240) → scaling_enable = true
// → VIN UDS 缩放 1024→320, 600→240 (bilinear) → 内存正常
```

**QVGA 正常 = 摄像头源数据正常 + MIPI 接收正常。1024×600 异常 = VIN bypass 路径有问题。**

### 3.2 根因假设（按可能性排序）

#### 假设 A（概率 ~50%）：VIN 在 scaling_enable=false 时 UDS 配置位仍然生效

`vin_scale_image()` 在 `scaling_enable = false` 时，只禁用了 scaling 使能位，但**没有清除或重设 UDS 控制寄存器**。以下配置来自 `common_data.c:515-529`：

```c
.uds_ctrl_bits.ne_bcb = 1,           // Near-Edge handling for Cb channel
.uds_ctrl_bits.ne_gy = 1,            // Near-Edge handling for Y channel
.uds_ctrl_bits.ne_rcr = 1,           // Near-Edge handling for Cr channel
.uds_ctrl_bits.pixel_interpolation = 0,     // ← 可能关键
.uds_ctrl_bits.bilinear_advanced = 0,
.uds_ctrl_bits.scale_up_pixel_count = 1,

.uds_scale_bits.vertical_mask = 4096,       // 1.0x (12-bit fixed point)
.uds_scale_bits.horizontal_mask = 4096,     // 1.0x

.uds_bwidth_bits.bwidth_v = 64,             // 垂直带宽 = 64
.uds_bwidth_bits.bwidth_h = 64,             // 水平带宽 = 64

.uds_clipping_bits.cl_vsize = 600,
.uds_clipping_bits.cl_hsize = 1024,
```

**推测**：当 UDS 缩放被 bypass 后，VIN 的 color space conversion (CSC) 输出直接写入 SDRAM。但 `uds_bwidth`（带宽=64 像素/块）和 `uds_clipping` 可能在内部仍被用于界定"每行有效像素数"。如果 VIN 内部在 bypass 模式下错误地将 `bwidth_h=64` 当作每行的处理单元大小，1024 像素可能被当作 64 像素的块处理，但由于某种硬件 bug/边界条件，实际只处理了 512 个源像素并重复填充。

**但问题是：瑞萨例程也走同样的 bypass 路径。如果这是 VIN 硬件 bug，瑞萨例程也应该有同样的问题。**

#### 假设 B（概率 ~35%）：OV5640 寄存器配置不完整 → ISP 输出 ≠ DVPHO

虽然 QVGA 正常证明摄像头源数据"基本正确"，但不能排除一种情况：**ISP 实际输出的有效水平像素少于 1024，但在 QVGA 的 3.2× 下采样中，UDS 的 bilinear 插值掩盖了右侧填充/重复。**

`ov5640_mipi[]` 寄存器配置表**缺失**以下关键寄存器：

| 未配置寄存器 | 地址 | 作用 | 默认值推测 |
|-------------|------|------|-----------|
| HS, VS, HW, VH | 0x3800-0x3807 | 传感器读出窗口 | 全分辨率 2592×1944 |
| X_OFFSET, Y_OFFSET | 0x3810-0x3811 | ISP 输出偏移 | 0 |
| ISP_X_OUTPUT_SIZE | 0x3812 | **ISP 实际水平输出** | 未知（可能 ≠ 1024） |
| ISP_Y_OUTPUT_SIZE | 0x3813 | **ISP 实际垂直输出** | 未知（可能 ≠ 600） |

如果 `ISP_X_OUTPUT_SIZE` 的默认值 < 1024（如 640 或 512），OV5640 的 formatter 会填充/重复右侧像素到声明的 DVPHO=1024：

```
Sensor → ISP → (640 pixels) → Formatter: "需要 1024 pixels" → 重复填充 → MIPI TX
                                                          ↓
                                              640 real + 384 repeated
```

在 QVGA 模式（3.2× 下采样 + bilinear）中，这 384 个重复像素被 UDS 插值平均化，不易察觉。
在 1024×600 bypass 模式中，重复像素直接透传，清晰可见。

**瑞萨例程为什么正常？** 可能瑞萨使用的 OV5640 模组固件/默认寄存器值恰好使 ISP_X_OUTPUT_SIZE = 1024。或者出厂 OV5640 模组的默认值与用户的模组不同。

#### 假设 C（概率 ~15%）：VIN YCbCr→RGB CSC 在 stride=1024 时的边界处理

VIN 做 YCbCr422→RGB565 转换时，以像素对（每对 4 字节输入 → 4 字节输出）为单位处理。1024 像素 = 512 像素对，整除无余数。但如果 CSC 硬件内部使用某种 block-based 处理（如 64 像素块），且 1024 不是其内部 buffer 的整数倍...

这个假设较弱，因为 stride=1024 是 2 的幂，对齐良好。

---

## 4. 综合诊断结论

```
┌──────────────────────────────────────────────────────────────────────┐
│                         最终诊断结论 (v2)                             │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ★ 已排除：MIPI 带宽不足（738 Mbps > 590 Mbps, +20% 余量）           │
│  ★ 已排除：GLCDC 显示端（帧缓冲数据本身就是重复的）                   │
│  ★ 已排除：VIN UDS 缩放路径（QVGA 模式正常）                          │
│  ★ 已排除：Cache 一致性问题（Write-Through 已使能）                   │
│                                                                      │
│  ★ 核心问题：VIN 在 1:1 bypass 模式下的帧缓冲区数据出现水平重复       │
│                                                                      │
│  ★ 最可能根因：OV5640 ISP 输出尺寸 (0x3812/0x3813) 未显式配置，       │
│     ISP 实际输出 < DVPHO(1024)，formatter 重复填充。                  │
│     QVGA 模式下 UDS bilinear 插值掩盖了此问题。                       │
│                                                                      │
│  ★ 次要可能：VIN bypass 模式下 UDS 配置位（bwidth/clipping）的        │
│     意外交互，但与瑞萨例程相同配置 → 需进一步验证。                    │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 5. 建议修复方案

### 方案 A（最推荐）：全链路改为 800×480 1:1 映射

```
摄像头输出 800×480 → VIN 1:1 bypass → GLCDC 1:1 显示
好处：绕过所有缩放不确定性，MIPI 带宽余量增至 ~60%
```

关键修改项：
1. `camera_sensor.h`: `CAMERA_IMAGE_WIDTH=800`, `CAMERA_IMAGE_HEIGHT=480`
2. `common_data.c`: VIN preclip → 800×480, UDS clipping → 800×480
3. `common_data.h`: `VIN_CFG_IMAGE_STRIDE=800`, `DISPLAY_HSIZE_INPUT0=800`, `DISPLAY_VSIZE_INPUT0=480`
4. `common_data.c`: FB 数组大小自动调整
5. 建议同时 `FPS_TARGET=30` 或 `45`（降低带宽需求更保险）

### 方案 B：补全 OV5640 ISP 寄存器（最小改动）

在 `ov5640_mipi[]` 数组的 `TIMING_DVPHO/DVPVO` 之后添加：

```c
/* ISP input window (full sensor) */
{0x3800, 0x00}, {0x3801, 0x00},  // HS = 0
{0x3802, 0x00}, {0x3803, 0x04},  // VS = 4
{0x3804, 0x0a}, {0x3805, 0x20},  // HW = 2592
{0x3806, 0x07}, {0x3807, 0x98},  // VH = 1944

/* ISP output offset */
{0x3810, 0x00},  // X_OFFSET = 0
{0x3811, 0x00},  // Y_OFFSET = 0

/* ISP output size — MUST match DVPHO/DVPVO */
{0x3812, (uint8_t)(CAMERA_IMAGE_WIDTH>>8)},
{0x3812 + 1, (uint8_t)(CAMERA_IMAGE_WIDTH)},
{0x3813, (uint8_t)(CAMERA_IMAGE_HEIGHT>>8)},
{0x3813 + 1, (uint8_t)(CAMERA_IMAGE_HEIGHT)},
```

> ⚠️ **注意**：0x3812 和 0x3813 的寄存器偏移地址在不同 OV5640 版本中可能不同。**必须对照你手上的 OV5640 datasheet 确认寄存器地址**。部分版本中 ISP 输出尺寸寄存器可能是连续地址（如 0x3812-0x3813 各 8 位组成 16 位值），也可能是分开的。

### 方案 C：让 VIN UDS 始终使能（不 bypass）

在 `vin_scale_image()` 中，即使 input==output，也强制使能 UDS（用 1.0x 的 scale mask 走 UDS 路径而不是 bypass）：

```c
// 修改 vin_scale_image() 的结尾逻辑：
// 始终使能 scaling，即使 input==output
g_vin_cfg_run_time_extend.input_ctrl.cfg_bits.scaling_enable = true;
// mask=4096 已经是 1.0x，UDS 在 1.0x 时等效于 bypass 但走的是 UDS 数据路径
```

这个方案的思路是：既然 QVGA（UDS 使能）数据正常，而 1024×600（UDS bypass）数据异常，那就让所有分辨率都走 UDS 路径，包括 1:1 的情况。用 `mask=4096` 在 UDS 中实现 1:1 映射，数据路径与 QVGA 一致。

---

## 6. 快速验证步骤（不修改代码）

### 步骤 1：I2C 读回 OV5640 ISP 寄存器 ⭐ 最优先

在 `camera_open()` 之后添加调试代码（通过 RTT 或串口）：

```c
uint8_t val_h, val_l;

// Read ISP x_output_size (0x3812)
read_reg_16bit(0x3812, &val_h);
read_reg_16bit(0x3812 + 1, &val_l);
APP_PRINT("ISP X_OUT: %d\r\n", ((uint16_t)val_h << 8) | val_l);

// Read ISP y_output_size (0x3813)  
read_reg_16bit(0x3813, &val_h);
read_reg_16bit(0x3813 + 1, &val_l);
APP_PRINT("ISP Y_OUT: %d\r\n", ((uint16_t)val_h << 8) | val_l);

// Also read DVPHO/DVPVO for comparison
read_reg_16bit(0x3808, &val_h);
read_reg_16bit(0x3809, &val_l);
APP_PRINT("DVPHO: %d\r\n", ((uint16_t)val_h << 8) | val_l);

read_reg_16bit(0x380A, &val_h);
read_reg_16bit(0x380B, &val_l);
APP_PRINT("DVPVO: %d\r\n", ((uint16_t)val_h << 8) | val_l);
```

**预期判断**：
- 如果 ISP_X_OUT ≠ DVPHO(1024) → 确认根因 B，ISP 输出不匹配
- 如果 ISP_X_OUT == 1024 → 排除 ISP 寄存器问题，聚焦 VIN 路径

### 步骤 2：Color Bar 测试模式

将 `mipi_csi.c` 中的 `&live_camera` 临时改为 `&color_bar_test_pattern`：

```c
err = camera_write_array(&color_bar_test_pattern);  // 代替 &live_camera
```

Color bar 由 OV5640 内部纯数字生成（不经过 ISP 图像处理链路），直接输出到 MIPI TX。
- Color bar 在 1024×600 下也重复 → 问题在 MIPI TX 或 VIN 接收
- Color bar 在 1024×600 下正常（1024 像素都有独立颜色） → 问题在 ISP 链路（验证了根因 B）

### 步骤 3：强制 UDS 使能测试（方案 C 验证）

临时修改 `vin_scale_image()` 的末尾：

```c
// 临时修改：始终使能 scaling
g_vin_cfg_run_time_extend.input_ctrl.cfg_bits.scaling_enable = true;
// mask=4096 已经是 1.0x，无需修改

// 原代码：
// if ((input_height != new_height) || (input_width != new_width))
// { scaling_enable = true; }
// else
// { scaling_enable = false; }
```

如果强制 UDS 使能后 1024×600 显示正常 → 确认 VIN bypass 路径有 bug，采用方案 C 修复。

---

## 7. 避坑指南

### 7.1 OV5640 寄存器避坑

| 坑 | 说明 |
|----|------|
| **ISP 与 DVP 双重输出尺寸** | OV5640 有两套输出尺寸寄存器：ISP 输出 (0x3812/0x3813) 和 DVP/MIPI 输出 (0x3808-0x380B)。两者必须匹配，否则 formatter 进行填充/裁剪。`ov5640_mipi[]` 只配置了后者。 |
| **0x3812/0x3813 地址需确认** | OV5640 的寄存器映射有多个版本。确认你的 datasheet 中 ISP output size 寄存器地址。部分版本中 0x3812 可能是 "X offset for ISP output"，而不是 size。 |
| **XINC/YINC 子采样** | X_INC=0x31 (odd=1, even=3) → 约 1/2 水平子采样。修改分辨率时需同步调整。 |

### 7.2 VIN 避坑

| 坑 | 说明 |
|----|------|
| **scaling_enable 不等于完整的 UDS bypass** | `scaling_enable=false` 绕过 UDS 缩放核，但 UDS 配置寄存器（bwidth, clipping, NE）可能仍影响数据路径的其他部分（如输出格式化）。 |
| **preclip 0-based vs clipping 1-based** | preclip.pixel_end=1023 → 1024 pixels；uds_clipping.cl_hsize=1024 → 1024 pixels。修改时注意数值含义不同。 |
| **image_stride 单位是像素** | `VIN_CFG_IMAGE_STRIDE=1024` 表示每行 1024 个像素。对于 RGB565 (2 bytes/pixel) = 2048 bytes/line。此值必须与 GLCDC hstride 表示的字节数一致。 |

### 7.3 GLCDC 800×480 适配避坑

| 坑 | 说明 |
|----|------|
| **hstride 自动对齐** | `glcdc_init()` 中 hstride 强制 4 字节对齐。800×2=1600，已对齐。 |
| **Panel Clock 计算** | GLCDCCLK = 240 MHz, divisor=1/9 ≈ 26.67 MHz。800×480 时序 (816×496 total) → ~66 fps。确认 LCD panel datasheet 支持的像素时钟范围和典型值。 |
| **Aspect Ratio 差异** | 1024/600≠800/480。如果保持 1024×600 输入，GLCDC hardware scaler 会产生非等比缩放。建议输入输出统一为 800×480。 |

---

## 8. 总结

| 问题 | 答案 |
|------|------|
| **是 OV5640 捕获问题吗？** | **可能是。** OV5640 ISP 输出尺寸寄存器 (0x3812/0x3813) 未配置，ISP 实际输出可能小于 DVPHO=1024，导致 formatter 填充重复像素。 |
| **是缩放算法问题吗？** | **间接相关。** VIN UDS 缩放路径（QVGA 模式）工作正常；VIN 1:1 bypass 路径（1024×600 模式）出现重复。需要进一步验证 bypass vs UDS 路径差异。 |
| **是 LCD 显示问题吗？** | **不是。** GLCDC 800×480 时序正确，显示通路工作正常。帧缓冲数据本身就是重复的。 |
| **MIPI 带宽够吗？** | **够。** 实际 738 Mbps > 需要 590 Mbps，20% 余量。瑞萨官方例程证实。 |
| **最优先的调试步骤** | **步骤 1**：I2C 读回 0x3812/0x3813 (ISP output size)，与 DVPHO=1024 对比。**步骤 2**：Color bar 测试模式区分 ISP 问题 vs MIPI TX 问题。**步骤 3**：强制 UDS 使能测试区分 bypass 路径问题。 |
